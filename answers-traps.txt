Q: Which registers contain arguments to functions?
A: a0~a7

Q; which register holds 13 in main's call to printf?
A: a2

Q: Where is the call to function f in the assembly code for main? Where is the call to g? 
A: "li	a1,12" compiler computes f(8) + 1 already.

Q: At what address is the function printf located?
A: Ox630

Q: What value is in the register ra just after the jalr to printf in main?
A: Ox38(not sure)


Q: In the following code, what is going to be printed after 'y='? (note: the answer is not a specific value.) Why does this happen?
	printf("x=%d y=%d", 3);
A: y could be any int value.